/**
 * Registered marketing / campaign entry shells. Add a new id when you add a folder
 * under `src/entry-points/<id>/` and wire it in `ActiveMarketingEntry.tsx`.
 */
export type MarketingEntryId = "fake-xero-com" | "none";
