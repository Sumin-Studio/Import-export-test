/**
 * XDL `typography/fontFamily/base` — production Bills / web UI (Helvetica Neue stack).
 * Cashflow Actions prototype uses this for body (`--font-body`) and Highcharts.
 */
export const PRODUCT_FONT_STACK =
  '-apple-system, BlinkMacSystemFont, "Segoe UI", Roboto, Oxygen-Sans, Ubuntu, Cantarell, "Helvetica Neue", sans-serif';

/** Alias for chart modules that imported the old name. */
export const NATIONAL_FONT_STACK = PRODUCT_FONT_STACK;
