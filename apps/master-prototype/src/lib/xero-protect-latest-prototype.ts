/**
 * Deep links for “latest Protect” UX. In this standalone app they stay on the local
 * `/bills` surface (the share bundle has no nested `/xero-protect/prototype/...` hub).
 */
export const XERO_PROTECT_LATEST_PROTOTYPE_SLUG = "local";

export const XERO_PROTECT_LATEST_ROOT = "/bills";

export const XERO_PROTECT_LATEST_BILLS = "/bills";
