/**
 * Hub entry points and deep links should target the current shipped Xero Protect
 * prototype. Bump when a newer prototype becomes the default.
 */
export const XERO_PROTECT_LATEST_PROTOTYPE_SLUG = "9";

export const XERO_PROTECT_LATEST_ROOT = `/xero-protect/prototype/${XERO_PROTECT_LATEST_PROTOTYPE_SLUG}`;

export const XERO_PROTECT_LATEST_BILLS = `${XERO_PROTECT_LATEST_ROOT}/bills`;
