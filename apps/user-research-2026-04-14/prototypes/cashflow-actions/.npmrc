# Force public npm when installing from this folder; Bun may not apply the repo-root
# .npmrc, and ~/.npmrc often points corporate Artifactory at unscoped `react` / `next`.
registry=https://registry.npmjs.org/
