{
  "name": "frame-of-mind",
  "version": "1.0.0",
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "start": "bash run.sh",
    "dev": "bash -c 'export PATH=\"$HOME/.bun/bin:$PATH\" && bun run --watch server.ts'",
    "build": "vite build",
    "preview": "vite preview"
  },
  "devDependencies": {
    "vite": "^6.0.0"
  },
  "dependencies": {
    "gifenc": "^1.0.3"
  }
}
