import { defineConfig } from "vite";

// For GitHub Pages: if your site is at https://user.github.io/repo-name/, set base to '/repo-name/'
// For Vercel or custom domain: use default '/'
export default defineConfig({
  base: "/",
});
