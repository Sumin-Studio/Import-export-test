{
  "buildCommand": "npm install && npm run build",
  "outputDirectory": "build/client",
  "framework": null,
  "rewrites": [
    { "source": "/api/:path*", "destination": "/api/:path*" },
    { "source": "/((?!assets|apps\\.json|api).*)", "destination": "/index.html" }
  ]
}
